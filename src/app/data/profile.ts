/**
 * profile.ts — SUPPRIMÉ, fusionné dans UserService
 * Compatibilité transitoire — migrez vers import { UserService, ... } from './user'
 */
export { UserService as ProfileService } from './user';
export type { Education, Language } from './user';
